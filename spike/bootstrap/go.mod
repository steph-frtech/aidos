// THROWAWAY — DP10 spike. Confined to /spike/bootstrap/ (KRD §84: ratchet OFF, rigor T0).
// A standalone module so the probe never imports nor pollutes the real back/ module.
// Nothing here graduates: /harvest proposes, /goal freezes, the real code is rebuilt later (DP12+).
module aidos.spike/bootstrap

go 1.22
