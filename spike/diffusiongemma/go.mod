// THROWAWAY — DG01 spike. Confined to /spike/diffusiongemma/ (KRD §84: ratchet OFF, rigor T0).
// A standalone module so the probe never imports nor pollutes the real back/ module.
// Nothing here graduates: /harvest proposes, /goal freezes, the real code is rebuilt later (DG02+).
module aidos.local/spike/diffusiongemma

go 1.22
