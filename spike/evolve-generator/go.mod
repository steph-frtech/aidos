// THROWAWAY — EG01 spike. Confined to /spike/evolve-generator/ (KRD §84: ratchet OFF,
// rigor T0). A STANDALONE module so the probe never imports nor pollutes the real back/
// module. Nothing here graduates: /harvest proposes, /goal freezes; the real generator
// (if GO) is wired behind the existing back/mcp/evolve seam at EG02+, rebuilt — not lifted.
module aidos.local/spike/evolve-generator

go 1.22
