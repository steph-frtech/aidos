// THROWAWAY — EL01 spike. Confined to /spike/besoin/ (KRD §84: ratchet OFF, rigor T0).
// A standalone module so the probe never imports nor pollutes the real back/ module.
// Nothing here graduates: /harvest proposes, /goal freezes, the real code is built later
// (EL02+ — the pkg back/runtime/besoin). The wall is intact (no kernel/mirrors/fitness write).
module aidos.spike/besoin

go 1.22
