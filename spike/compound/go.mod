// THROWAWAY — CE01 spike. Confined to /spike/compound/ (KRD §84: ratchet OFF, rigor T0).
// A standalone module so the probe never imports nor pollutes the real back/ module.
// Nothing here graduates: /harvest proposes, /goal freezes, the real code is rebuilt later (CE02+).
module aidos.spike/compound

go 1.22
