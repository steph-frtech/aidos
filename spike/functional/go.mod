// THROWAWAY — FN01 spike. Confined to /spike/functional/ (KRD §84: ratchet OFF, rigor T0).
// A standalone module so the probe never imports nor pollutes the real back/ module.
// Nothing here graduates: /harvest proposes, /goal freezes, the real emitters are
// re-shaped at FN02+. The wall is intact (no kernel/mirrors/fitness write).
module aidos.spike/functional

go 1.22
