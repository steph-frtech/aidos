// CODE GENERATED BY AIDOS — DO NOT EDIT. source: 1c2492192bd64975cfbb71079ee3f432e2bfb3cbb0b44f070c603fc8182d970e
// API projection (S36): POST /orders — operation "createOrder" over entity "Order".
package apigen

import (
	"encoding/json"
	"github.com/jackc/pgx/v5/pgtype"
	"net/http"

	"github.com/steph-frtech/aidos/back/kernel/operation"
)

// OrderRequest is the JSON request body for POST /orders. One field per
// Order attribute (S35 entity source), in source order — never re-typed.
type OrderRequest struct {
	Id        int64              `json:"id"`
	Customer  string             `json:"customer"`
	Total     pgtype.Numeric     `json:"total"`
	Discount  pgtype.Numeric     `json:"discount,omitempty"`
	Placed_at pgtype.Timestamptz `json:"placed_at"`
}

// OrderResponse is the JSON response body — the created Order resource.
type OrderResponse struct {
	Id        int64              `json:"id"`
	Customer  string             `json:"customer"`
	Total     pgtype.Numeric     `json:"total"`
	Discount  pgtype.Numeric     `json:"discount,omitempty"`
	Placed_at pgtype.Timestamptz `json:"placed_at"`
}

// OrderHandler is the generated transport adapter for POST /orders.
// It decodes the JSON request, delegates the body to the S10 operation
// interpreter (operation.Interpret of "createOrder"), and encodes the result as JSON.
// It re-implements no business rule and re-types no entity.
func OrderHandler(op operation.Operation, deps operation.Deps) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		if r.Method != "POST" {
			http.Error(w, "method not allowed", http.StatusMethodNotAllowed)
			return
		}
		var req OrderRequest
		if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
			http.Error(w, "bad request", http.StatusBadRequest)
			return
		}
		input := map[string]any{
			"id":        req.Id,
			"customer":  req.Customer,
			"total":     req.Total,
			"discount":  req.Discount,
			"placed_at": req.Placed_at,
		}
		state := operation.NewState(input, map[string]any{})
		_, result, err := operation.Interpret(op, state, deps)
		if err != nil {
			http.Error(w, "operation failed", http.StatusUnprocessableEntity)
			return
		}
		resp := OrderResponse{
			Id:        req.Id,
			Customer:  req.Customer,
			Total:     req.Total,
			Discount:  req.Discount,
			Placed_at: req.Placed_at,
		}
		_ = result
		w.Header().Set("Content-Type", "application/json")
		w.WriteHeader(http.StatusCreated)
		_ = json.NewEncoder(w).Encode(resp)
	}
}
