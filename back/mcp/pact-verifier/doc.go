// Package pactverifier is the AIDOS MCP server scaffold for verifying a Pact
// contract between cells (provider verification). Activated at S36.
//
// SCAFFOLD ONLY (ADR 0009 honesty guard): this is a declared spec, not an
// active server. It carries no logic and registers no tools until S36 turns the
// scaffold into a working `mcp.Server` with its fault-injection test. The
// working reference is back/mcp/store/main.go.
package pactverifier
