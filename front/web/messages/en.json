{
	"common": {
		"appName": "AIDOS Workbench",
		"tagline": "AIDOS's governance cockpit",
		"language": "Language",
		"live": "LIVE · Postgres",
		"demo": "demo data",
		"readOnly": "read-only",
		"loading": "Loading…",
		"backHome": "Home"
	},
	"nav": {
		"home": "Home",
		"contract": "Execution contract",
		"store": "Content store",
		"records": "Records"
	},
	"home": {
		"eyebrow": "AI development operating system",
		"title": "AIDOS Workbench",
		"subtitle": "The AI development OS, KRD method. Truth is yours; the agent writes the implementation; the harness holds the wall.",
		"panels": "Panels",
		"panelsLead": "Explore the read-only projections of the truth AIDOS governs.",
		"explore": "Open panel",
		"contractTitle": "Execution contract",
		"contractDescription": "The per-step loop and granularity rule, versioned and machine-readable.",
		"storeTitle": "Content store",
		"storeDescription": "The Archive's content-addressed, append-only store: objects by hash, head pointer, and head history.",
		"footer": "Read-only projections. Truth lives in Postgres, out of the agent's reach; writes flow through an approved changeset."
	},
	"store": {
		"eyebrow": "Archive · content store",
		"title": "Content store",
		"intro": "Content-addressed, append-only. Objects are stored by SHA-256 hash; editing produces a new hash while the old one stays readable. The head is a mutable pointer; its moves are an append-only history.",
		"liveTitle": "Read live from the aidos Postgres (schema archive)",
		"demoTitle": "Postgres unreachable — showing the demo fixture",
		"objectsHeading": "Objects ({count})",
		"headsHeading": "Heads ({count})",
		"byteUnit": "{count} B",
		"headBadge": "head: {key}",
		"currentBodyLabel": "Current body of head",
		"hashLabel": "hash",
		"historyHeading": "History — oldest first",
		"parentLabel": "parent:",
		"footer": "Source: live <code>archive</code> schema in Postgres. The panel can now write — but only the content store, <em>below</em> the wall (append-only); truth (kernel/mirrors/fitness) stays out of reach. The canonical door remains the <code>store</code> MCP server (ADR 0009).",
		"actionsHeading": "Actions — below the wall",
		"putHeading": "Store content",
		"putHint": "Content is stored by SHA-256 hash (idempotent). Identical input returns the same hash with no new row.",
		"putContentLabel": "Content",
		"putContentPlaceholder": "Enter the bytes to store…",
		"putSubmit": "Store",
		"putOk": "Stored under hash {hash}.",
		"putEmpty": "Enter non-empty content.",
		"setHeadHeading": "Move a head",
		"setHeadHint": "Point a head key at an existing hash and append a history row. Use the hash returned by a recent store.",
		"setHeadKeyLabel": "Head key",
		"setHeadKeyPlaceholder": "e.g. doc",
		"setHeadHashLabel": "Hash (hex)",
		"setHeadHashPlaceholder": "SHA-256 hash…",
		"setHeadUseLastPut": "Use last stored",
		"setHeadSubmit": "Move head",
		"setHeadOk": "Head \"{key}\" moved to {hash}.",
		"setHeadEmpty": "Provide a key and a hash.",
		"setHeadFailed": "Move failed (the hash must already exist in the store).",
		"dbUnreachable": "Postgres unreachable — write not possible. No data changed.",
		"resultLabel": "Result",
		"working": "Working…",
		"tutorial": {
			"badge": "Tutorial",
			"heading": "Understanding the content store",
			"lead": "The concepts behind this screen, before the jargon. The store keeps bytes and never destroys anything.",
			"t1": {
				"title": "Content-addressing",
				"body": "An object is named not by a label or a number, but by its content itself. The same byte sequence always yields the same address; two different contents have two different addresses."
			},
			"t2": {
				"title": "The SHA-256 fingerprint",
				"body": "That address is the SHA-256 fingerprint of the content — a hexadecimal digest computed over the bytes. It is both the storage key and the integrity proof: if the content changes, the fingerprint changes."
			},
			"t3": {
				"title": "An object is immutable",
				"body": "Once stored under its fingerprint, an object never moves again. Its content stays readable as-is, indefinitely. You don't overwrite an object: you store a new one."
			},
			"t4": {
				"title": "The head, a mutable pointer",
				"body": "The head is a mutable label binding a readable key (e.g. \"doc\") to a fingerprint. It is the only moving part: you point it at another fingerprint to \"advance\" the current version."
			},
			"t5": {
				"title": "Append-only history, idempotent Put",
				"body": "Every head move is appended to an append-only history: nothing is erased, you only stack. Put is idempotent — identical content returns the same fingerprint, with no new row. Editing makes a NEW fingerprint; the old one stays readable. Nothing is ever destroyed."
			}
		},
		"example": {
			"badge": "Example",
			"heading": "Try this",
			"lead": "A worked example that leans on the \"Store\" and \"Move a head\" controls below. Reproduce it step by step.",
			"e1": {
				"title": "1) Store a first content",
				"body": "In \"Store content\", enter <code>hello</code> and submit. You get its SHA-256 fingerprint: that is the address of the first object."
			},
			"e2": {
				"title": "2) Point the \"doc\" head at it",
				"body": "In \"Move a head\", set the key <code>doc</code>, click \"Use last stored\" to reuse the fingerprint, then move the head. The <code>doc</code> head now names <code>hello</code>."
			},
			"e3": {
				"title": "3) Edit: store \"hello v2\"",
				"body": "Store <code>hello v2</code> (a new fingerprint), then move the <code>doc</code> head onto it. The head moves, the history now lists both moves, and the fingerprint of <code>hello</code> stays readable."
			},
			"takeaway": "Takeaway: editing overwrites nothing. A new fingerprint appears, the head advances, the history grows append-only — and every past version stays reachable by its fingerprint."
		}
	},
	"contract": {
		"eyebrow": "Runtime · execution contract",
		"title": "Execution contract",
		"phasesHeading": "Per-step loop — {count} phases",
		"colId": "id",
		"colPhase": "Phase",
		"colGate": "Gate",
		"granularityHeading": "Granularity rule — {count} properties",
		"footer": "Source: <source>docs/implementation_contract.md</source> — read-only projection. Changes require a ChangeSet (DRAFT → APPLIED) and a semver bump.",
		"tutorial": {
			"badge": "Tutorial",
			"heading": "Understanding the execution contract",
			"lead": "The concepts behind this screen, before the jargon. The contract is the root law every later step must obey — one file, two readers.",
			"t1": {
				"title": "One file, two readers",
				"body": "The contract lives in <code>docs/implementation_contract.md</code>: a machine-readable YAML block (the harness reads it) topped by human-readable prose. Both say the same thing; the YAML is the canonical source, the prose is its projection."
			},
			"t2": {
				"title": "The per-step loop — nine phases",
				"body": "Every AIDOS step follows the same ordered loop: <code>grill-with-docs</code> → BDD mirror → <code>tdd</code> → sensors → completeness → <code>diagnose</code> → UI+Playwright → improve-architecture → artifacts. You never skip a phase."
			},
			"t3": {
				"title": "Gate: computational or human",
				"body": "Each phase carries a <code>gate</code>: <code>computational</code> if the harness can decide it alone, <code>human</code> if it needs a human judgement. That is what separates what self-certifies from what needs a decision."
			},
			"t4": {
				"title": "The granularity rule — five properties",
				"body": "Every step is <code>minimal</code> (one capability), <code>autonomous</code> (testable alone), <code>visualizable</code> (a route + an e2e), <code>non-destructive</code> (never silently breaks) and <code>chainable</code> (leaves a stable phase)."
			},
			"t5": {
				"title": "\"Done is computed, never declared\"",
				"body": "You never declare a step done: it is computed — red set green ∧ prior green intact ∧ no monster. Any change to the contract goes through a ChangeSet and a semver bump; the screen only projects, it never writes."
			}
		},
		"example": {
			"badge": "Example",
			"heading": "What a phase looks like",
			"lead": "A concrete example: the shape of one <code>phase</code> entry of the machine block — its <code>id</code>, <code>label</code> and <code>gate</code>. No input required; you read the contract as it is encoded.",
			"idLabel": "id",
			"idValue": "tdd",
			"idNote": "stable, unique identifier",
			"labelLabel": "label",
			"labelValue": "/tdd to code",
			"gateLabel": "gate",
			"gateValue": "computational",
			"gateNote": "decidable by the harness",
			"bodyLabel": "Machine block (canonical YAML)",
			"body": "- id: tdd\n  label: \"/tdd to code\"\n  description: >\n    red → green → refactor, in the step's own package.\n  gate: computational",
			"takeaway": "Takeaway: every phase carries the same envelope — <code>id</code>, <code>label</code>, <code>gate</code> — and the table below is nothing more than the projection of these entries. To evolve the contract you don't edit it here: you go through a ChangeSet (coming S20) and bump the semver."
		}
	},
	"records": {
		"eyebrow": "Kernel · KRDCore records",
		"title": "Records",
		"intro": "The seven KRDCore record kinds — content-addressed, append-only, JSONB. These tables are truth, ABOVE the wall: the agent role has SELECT only. This screen reads them; it never writes any. The only write door is the governed propose → ChangeSet → human-approval flow (coming, S20).",
		"liveTitle": "Read live from the aidos Postgres (schemas ideas/kernel/mirrors/changesets/dag)",
		"demoTitle": "Postgres unreachable — showing the demo examples",
		"typesHeading": "Record types ({count})",
		"kind": {
			"idea": "Idea",
			"truth": "Truth",
			"mirror": "Mirror",
			"layer": "Layer",
			"link": "Link",
			"changeset": "ChangeSet",
			"phase": "Phase"
		},
		"aboveBadge": "above the wall",
		"belowBadge": "below the wall",
		"aboveTitle": "Human-anchored truth, out of the agent's reach — written only via an approved ChangeSet.",
		"belowTitle": "Candidate below the freeze (Idea) — promoted toward truth via the idea → mirror → /goal flow.",
		"headCount": "{count} head(s) shown of {total}",
		"emptyTable": "No records yet.",
		"idLabel": "id",
		"versionLabel": "version",
		"supersededLabel": "superseded by:",
		"proposeHeading": "Propose a record",
		"proposeHint": "The wall (CLAUDE.md §2): truth is never written directly from the screen. A write goes through propose → ChangeSet → human approval.",
		"proposeSubmit": "Open a ChangeSet",
		"soonBadge": "coming (S20)",
		"soonTitle": "The ChangeSet engine arrives at S20 (idea-intake at S27) — the control is present but disabled.",
		"proposeWhy": "Why is this disabled?",
		"proposeNote": "The governed write path — propose → ChangeSet → human approval — is not built yet (ChangeSet engine at S20, idea-intake at S27). The control is present to make the capability reachable, but disabled; it writes nothing. The wall holds: the screen never touches kernel/mirrors/ideas.",
		"footer": "Source: live <code>ideas</code>/<code>kernel</code>/<code>mirrors</code>/<code>changesets</code>/<code>dag</code> schemas in Postgres — READ-ONLY projection (the agent role has SELECT only). Truth is written only via an approved ChangeSet through the <code>aidos</code> role; never from the Workbench.",
		"tutorial": {
			"badge": "Tutorial",
			"heading": "Understanding the KRDCore records",
			"lead": "The concepts behind this screen, before the jargon. A record is a frozen truth, addressed by its content, never erased.",
			"t1": {
				"title": "Seven record families",
				"body": "AIDOS's truth is filed into seven families: the idea (<code>ideas.idea</code>), then in the kernel the truth, the layer and the link (<code>kernel.truth</code>/<code>layer</code>/<code>link</code>), the mirror (<code>mirrors.mirror</code>), the ChangeSet (<code>changesets.changeset</code>) and the DAG phase (<code>dag.phase</code>). Each has its own table, but all share the same record shape."
			},
			"t2": {
				"title": "Content-addressed truth",
				"body": "A record has no name: its <code>id</code> is the SHA-256 fingerprint of its canonical JSONB body. The same body always yields the same id; two different bodies have two different ids. The address is the content."
			},
			"t3": {
				"title": "Append-only: never overwrite",
				"body": "A body is never modified. To \"change\" a truth, you INSERT a new record and mark the old one <code>superseded_by</code> its successor. Nothing is destroyed: the head moves, the history stacks."
			},
			"t4": {
				"title": "Version = fingerprint = licence to change",
				"body": "The <code>version</code> is also a fingerprint of the content. It does not increment like a counter: it is recomputed. Knowing the current version is holding the licence to propose the next one — on the right base, with no collision."
			},
			"t5": {
				"title": "The wall: the screen never touches truth",
				"body": "These tables are ABOVE the line: the agent role has READ (SELECT) only. Every truth-write goes through propose → ChangeSet → human approval (<code>coming S20</code>). This screen reads the records; it never writes any."
			}
		},
		"example": {
			"badge": "Example",
			"heading": "What a record looks like",
			"lead": "A concrete example: the shape of a <code>kernel.truth</code> (a kernel truth). No input required — we show the addressing envelope and the JSONB body so the structure is clear.",
			"idHash": "a1b2c3d4e5f6…",
			"idNote": "= SHA-256 of the canonical body",
			"versionHash": "a1b2c3d4e5f6…",
			"versionNote": "fingerprint = licence to change",
			"supersededNote": "∅ = this is the head (not superseded)",
			"bodyLabel": "JSONB body (canonical)",
			"body": {
				"statement": "A record id is the SHA-256 fingerprint of its canonical body."
			},
			"takeaway": "Takeaway: every record carries the same envelope — <code>id</code>, <code>version</code>, <code>superseded_by</code>, <code>created_at</code> — plus a JSONB body specific to its family. To create a real one, you don't write it here: you go through propose → ChangeSet (coming S20)."
		}
	}
}
