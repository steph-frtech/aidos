import { KernelsScreen } from "./KernelsScreen";

/**
 * /v2/kernels — l'arbre fractal des kernels (WB2-04). La route de l'étape ; rend l'écran partagé.
 */
export default function V2KernelsRoute() {
	return <KernelsScreen />;
}
