# Mirror · runtime.generators.webcomponent.Emit · fixture (state → command → events)

- reflects: `runtime.generators.webcomponent.Emit`
- test_kind: `fixture`
- cert_language: `operation-dsl/go`
- liveness: `live`
- authority: `below` (the emitter is a projection below the waterline; the control/action
  specs it reads are SOURCE truth above the line, S11)

This is the materialized, human-readable form of the **web-projection emitter** fixture;
the runnable mirror is `back/runtime/generators/webcomponent/webcomponent_fixture_test.go`
(+ the `_property_test.go` ∀ slot and the `ledger_roundtrip_test.go` Testcontainers leg).
Conceptually this record lives in the `mirrors` Postgres schema and is persisted there at
S06 (bootstrap exception — until the back-fill the file + the Go tests ARE the red→green
proof).

It is the **lien porteur**: the tests load the control/action below; if the bind row
disappears the test breaks (no silent rot into a monster).

The control + action are the S11 anchors `control.CheckoutButton()` +
`action.CheckoutSubmit()`, **verbatim** — the emitter invents no view, label, condition,
trigger, `on`, or `invoke`.

## fixture: emitting a control-spec produces a protected, source-hashed Next component

```
state   (control): control "checkout-button" { view, label, visible_when, enabled_when, triggers: action "checkout-submit" }   # kernel (S11)
state   (action):  action "checkout-submit"  { on: click("checkout-button"), invoke: operation "createOrder", with{…}, on_success[], on_error[] }   # kernel (S11)
command (emit):    Emit(control, action, "ts-next")
events:  [ Emitted ]
  -> 1 artifact, target "ts-next", kind "control", path front/web/app/web-preview/_generated/checkout-button.tsx
  -> artifact.source_hash == Hash(Canonicalize(control.body ⊕ action.body))   # content-addressed, S02/S11 reused
  -> artifact.bytes start with "// CODE GENERATED BY AIDOS — DO NOT EDIT. source: <hash>"   # protected
  -> Emit(...) == Emit(...) byte-for-byte                                       # determinism (S34 contract)
  -> the INVOKE / LABEL embedded are VERBATIM from the action / control AST      # honesty — never invented
```

Anchored Go content hashes (the authoritative truth the TS twin mirrors):

- `source_hash = 8ebfb46ff10c0dc68ee53be000e9965466e7effa666703f870fce027fbae5cf6`
- `output_hash = 5b428d00a19c01d565697f076bf2770f34c3ab0ce32c379233e3f6fd68030f76`

## the red-set scenarios (block, never panic / invent)

| situation | expected |
|---|---|
| `control.triggers != action.Name` (orphan bind) | `BlockReason` (non-empty French how_to_fix) |
| `action.on.control != control.Name` / non-click `on` | `BlockReason` |
| malformed/empty control (no view / no condition) | `BlockReason` |
| target outside the closed set (e.g. `mobile`) | `BlockReason` |

## the e2e done criterion (front N0, playwright-bdd)

`tests/e2e/web-preview.feature` + `tests/e2e/web-preview.spec.ts` replay the S11 state
fixture (`given → {visible, enabled}`) and the action event fixture (`click → invoke`)
against the **rendered** emitted component on `/web-preview`. Green e2e = done.

> The ∀ slot (rapid) is carried by `webcomponent_property_test.go` (determinism;
> source_hash == Hash(Canonicalize(c.body ⊕ a.body)); the protected header; orphan-bind
> blocks). The TS reproducibility slot (fast-check) lives in
> `front/web/lib/web-projection.test.ts` + `aidos-expr.test.ts`, anchored on the Go hashes.
