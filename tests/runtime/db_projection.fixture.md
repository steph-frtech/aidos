# db_projection.fixture — S37 DB projection (Atlas migration emitted from the entity) + DataTruthScope

> mirrors schema · reflects: gen.db.{EmitMigration,RequireMigration,DataTruthScope}
> test_kind: schema-validation/fixture · cert_language: fixture · authority: above
> materialized to: back/gen/db/db_fixture_test.go (Go runner) + the Testcontainers dry-run
> (back/gen/db/migration_dryrun_test.go). The db layer is a PROJECTION (KRD line 532),
> ai / BELOW the waterline; its mirror is schema-validation by nature. These ARE the
> done criteria: the emitted migration dry-runs valid ∧ a historical-impact change
> requires a declared migration (§44.3).

## fixture A — the entity emits an expand-contract migration that dry-runs valid (done half 1)

```
state   (entity head + prior):  Order @prior (id,total,discount)  →  Order @new (+ coupon: text)
command (emit):                 EmitMigration(Order@new, prior=Order@prior)
events:  [ Emitted ]
-> output is an Atlas migration carrying the protected header `// CODE GENERATED BY AIDOS — DO NOT EDIT. source: <hash>`
-> source_hash == Hash(Canonicalize(SourceBody(Order@new)))  (S02 reused, never forked)
-> the additive change is an EXPAND step: `ALTER TABLE "order" ADD COLUMN "coupon" TEXT;` — no in-place rewrite/drop of existing rows
command (dry-run):              ephemeral Testcontainers Postgres: apply prior CREATE TABLE, then the emitted migration
events:  [ DryRunValid ]
-> re-emitting the same (Order@new, prior) gives byte-identical migration text (determinism)
```

## fixture A' — no prior ⇒ the migration is the CREATE TABLE expand step

```
state   (entity head, no prior): Order @new, prior = nil
command (emit):                  EmitMigration(Order@new, prior=nil)
events:  [ Emitted ]
-> output is the full `CREATE TABLE "order" (...)` (the S35 EmitDDL shape, reused not re-typed) under the protected header
command (dry-run):               ephemeral Testcontainers Postgres apply
events:  [ DryRunValid ]
```

## fixture B — a historical-impact change requires a declared migration (done half 2, §44.3)

```
state   (change):  a NARROWING change to Order (drop the `discount` column) — applies_to includes existing/historical_records, NO DataTruthScope declared
command (require): RequireMigration(change)
events:  [ Blocked ]
-> block_reason.code == "HISTORICAL_IMPACT_REQUIRES_MIGRATION"   (S13 BlockReason shape)
-> nothing is emitted/applied silently (the old truth over historical records is preserved)
```

## fixture C — a declared DataTruthScope unblocks the historical-impact change (the door, §44.3)

```
state   (change + scope):  the same narrowing change + DataTruthScope{
                             applies_to:[existing_records,historical_records],
                             migration:{required:true, strategy:expand_contract},
                             audit:{preserve_old_truth:true} }
command (require):         RequireMigration(change)
events:  [ Allowed ]
-> EmitMigration on the narrowing change is split expand→backfill→contract (forward-only), never a single destructive DROP
```

## fixture D — a new-records-only change needs no historical migration

```
state   (change):  applies_to == [ new_records ] only
command (require): RequireMigration(change)
events:  [ Allowed ]                                              # migration not required for new-only
```

## fixture E — the DataTruthScope id is the content hash of its body (content-addressed, S02 reused)

```
state   (scope):  DataTruthScope{ applies_to, migration, audit }
command (compute): scope.ID()
events:  [ scope.id == Hash(Canonicalize(body)) ]
```

## fixture F — an unknown migration strategy is rejected, not guessed (honesty)

```
state   (scope):  DataTruthScope{ migration:{ strategy:"teleport" } }   # not in the §44.3 closed set
command (validate): RequireMigration(change-with-this-scope)
events:  [ Blocked ]
-> block_reason.code == "UNKNOWN_MIGRATION_STRATEGY"   # closed set: expand_contract|backfill|dual_read|dual_write
```
