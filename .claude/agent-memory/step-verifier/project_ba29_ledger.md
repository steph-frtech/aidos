---
name: ba29-ledger
description: BA29 effect-log + fidelity-to-reality ledger (gap H2) — independent boundary Effect log + pure Reconcile set-diff; auditable = replayMatches ∧ reconciled; runtime.agent_effect append-only below the line; verified-green
metadata:
  type: project
---

BA29 = the fidelity-to-reality LEDGER closing gap H2. BA28 proved a run RE-DERIVES (hash-equality) but that only proves the RECORD is internally consistent, not that the REAL run matched it. BA29 adds an INDEPENDENT boundary effect-log (runtime.agent_effect, captured at the harness edge, NOT self-reported) and a pure `Reconcile(run, effects)` set-diff: unrecorded_effect = the betrayal (boundary saw a write the run never recorded), unrealised_action = a recorded write the boundary never saw. A REFUSED write (Autorisee=false) never reached the boundary → expects no effect. AUDITABLE = ReplayMatches ∧ Reconciled (the BA28+BA29 proofs together).

Files: back/runtime/agentloop/ledger.go (pure Reconcile/QueryLedger, total/deterministic), ledger_fixture_test.go (5 N2 cases), ledger_property_test.go (6 rapid props incl. reproducible + extra-effect-always-drifts), effect_log_roundtrip_test.go (4 Testcontainers: roundtrip, closed-kind CHECK, reconcile-over-real-rows, wall append-only via SET ROLE aidos_agent). Migration back/migrations/agent_effect_log_baseline.sql: runtime.agent_effect (run/kind∈{fs_write,mcp_call}/target, no version/mirror = below the line), GRANT SELECT+INSERT, REVOKE UPDATE/DELETE/TRUNCATE for aidos_agent + aidos (append-only). MCP agentloop_ledger tool (wrote_truth always false). Front twin lib/agentloop-ledger.ts (reconcile/queryLedger/refusalsOf, twin of Go) + agentloop-ledger.test.ts (10 fast-check). /agents Ledger subsection (data-testid 'ledger', Query button → ledger-result; faithful/refused/betrayed declared runs); e2e agents.spec.ts BA29 2 cases.

VERIFIED-GREEN: gofmt clean (the .sql gofmt errors are a false alarm — never pass .sql to gofmt), go vet/build clean, Testcontainers 4/4 PASS against real Postgres (forced -count=1, ~9.5s), short runtime+mcp all green (prior-green intact), tsc clean, biome clean, vitest 10/10, Playwright BA29 2/2 on :3000. 15 ba29* i18n keys present in both fr+en. Docs: concept + internals (3 layers) registered in docs.json, mint validate PASS, pushed to steph-frtech/docs main (commit d092018 on origin/main). Wall held: migration touches only runtime.agent_effect, no kernel/mirrors/fitness write. Determinism-first: Reconcile is a set-diff, refusal a tally — no LLM. OpenQuestions (non-blocking, fwd-deps): Linear MCP unauth (§11); live MCP/UI derive runs from declared fixtures not a real DSN (real adapter = downstream). See [[project_ba26_agentrun_replay]] [[project_ba28_replay_redact]].
