---
name: s117-cli-release
description: S117 final app-builder step — complete `aidos` CLI surface (goal/grill/spike/harvest/trim/init routed over S58 passerelle) + per-account Release-v0 content-addressed honest inventory; pure write-NOTHING; verified-green zero corrections
metadata:
  type: project
---

S117 = LAST app-builder step (§EPIC14, BUILDER_PLAN:455). Two done-criteria, BOTH proven:
(1) each new CLI verb has a red+green mirror; (2) release pack enumerates HONESTLY what EXISTS in account's live truth-store.

**CLI surface completion** (back/cmd/aidos/gateway.go + gateway_verbs_test.go): six verbs goal/grill/spike/harvest/trim/init wired as REAL sub-commands routed over S58 passerelle (runtime/gateway). verbToTool CLOSED map: goal→changeset_open (the ChangeSet door, ROUTES — proposes, never writes truth), grill→idea_grill, spike→idea_spike, harvest→idea_harvest, trim→idea_capture (DELETES NOTHING, opens idea), init→project_create. ALL six values verified present in gateway registry.go DefaultTools (below() lines 46/51/70). runGatewayVerb runs SAME deterministic gateway.DefaultRegistry().Route the HTTP edge runs. directTruthWriteVerbs (kernel-write/mirror-write/fitness-write → kernel_write/mirror_write/fitness_write DispositionTruthWrite registry:76-78) REFUSED with ChangeSet-pointing BlockReason → proves wall not bypassable via CLI hole. gatewayContracts SEPARATE list — Contracts() STILL returns exactly 5 core (S03 mirror unchanged), lookup() spans both. Tests: registry/tool-map/route-and-print/goal-routes-changeset-door/direct-truth-write-refused/determinism. main.go dispatch order: help → directTruthWrite check → lookup → gatewayVerb check → check/explain/diff/impact/stable.

**Per-account release pack** (back/runtime/adoption/accountrelease): GENERALISES S47 single-project release.Assemble to multi-project ACCOUNT. Assemble(AccountView, now) PURE TOTAL no-I/O → content-addressed AccountReleasePack: projects (each ProjectStatus real|demo HONEST consumed-never-editorialised), CLI/Workbench surface, docs index, test inventory (live mirrors), changelog, honest known-limits, + adoption.Plan (REUSED adoption.go:222, not duplicated) advising next tier. id=Hash(Canonicalize(canonicalBody)) REUSES krec.Canonicalize+Hash S01/S02, EXCLUDES id+assembled_at → two assemblies different times share id. Empty View → empty-but-valid pack (no fabrication). copy* helpers sort-stable by natural key → id independent of input order, never add/drop. adoption.Plan/Capability/AdoptionPlan verified-exist.

**Verification results** (zero corrections): go test cmd/aidos + accountrelease + mcp/release ALL ok; property test ROBUST -rapid.checks=3000 -count=2 3.6s (recurring-scar guard); gofmt CLEAN, vet CLEAN, broad `go build ./...` exit0; WALL grep (INSERT/UPDATE/Exec/pgx/sql.Open/os.WriteFile) on gateway.go+accountrelease.go+mcp main.go = CLEAN; prior-green runtime/gateway(S58)+runtime/adoption(S47) intact. MCP aidos-release 3 PURE read-only tools (release_assemble/release_adoption_plan/release_cli_surface) main_test green. TS twin lib/account-release.ts vitest 7/7 (fast-check determinism+id-excludes-assembledAt+no-mutation+no-fabrication). tsc: only 1 PRE-EXISTING error behavior-capture.test:101 'Kind' (S64-S77, NOT S117). biome clean on 4 new front files. i18n fr5007==en5007 EXACT, accountRelease 33==33. nav link WorkbenchHeader:196 k=accountRelease. Panel action-capable: ar-assemble button BINDS assemble() pure twin, ASSEMBLE-ONLY (no install/ship/publish — e2e asserts toHaveCount(0)), wall respected (local useState, writes nothing). Playwright 6/6 GREEN live :3000 route-200 (button executes, demo-vs-real status p-shop=real/p-demo=demo, core+6 gateway verbs, known-limits lim-doltgres/lim-async, next-tier T1→T2 / T0→T1, no-install). Docs 3-layer (Implémentation:9/Méta:52/Méta-méta:60) docs.json:301-302 mint validate PASS HEAD 4957164==origin/main pushed.

OQ (by-design forward-dep, NOT residual): OQ-S117-1 CLI gateway verbs render deterministic ROUTING decision over canonical replayable scope; live side-effecting dispatch (real S57 cookie + S61 identity + idea/project operand) is S58 gateway HTTP server seam — later wiring, consistent with check/diff/impact/stable READ-ONLY bootstrap surface. Linear unauth (linear-server MCP only authenticate/complete_authentication available in isolated executor) — OpenQuestion, don't fail.

verified-green ZERO corrections.
