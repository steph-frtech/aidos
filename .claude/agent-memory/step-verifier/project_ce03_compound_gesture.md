---
name: ce03-compound-gesture
description: CE03 = the /compound GESTURE — first functional capitalisation loop of the COMPOUND track; pure Compound(GoalClose)→Capture (proc memory write-input + draft idea via firewall.ViaIdea, WroteKernel always false).
metadata:
  type: project
---

CE03 (back/runtime/compound, follows [[ce02-capitalisation]] decision + [[ce01-compound-spike]]). Done-criteria: miroir fixture ROUGE d'abord ; un goal VERT produit une entrée procédurale + une idée draft (criterion says "Status=proposed") + AUCUNE écriture kernel (le mur). All met.

**Shape**: `Compound(GoalClose)(Capture,error)` — PURE, TOTAL. Green-gated (non-green ⇒ empty no-op). gesture_pattern → ONE `memory.WriteInput{Kind:KindProcedural,...,Branch}` (the CE02 procedural channel, S31). spec_pattern → ONE `firewall.IdeaCandidate` via `firewall.ViaIdea` (the ONLY legal door: idée→miroir→/goal). `Capture.WroteKernel()` iterates candidates, ALWAYS false (explicit+testable wall field). ids content-addressed (records.Hash∘CanonicalBody) so deterministic. Empty gesture pattern ⇒ no proc write but spec candidate still proposed. Provenance = "compound:<goalID>" on both events (KRD §119).

**The "proposed" criterion resolves to StatusDraft (NOT a gap)**: S27 ideas.Status enum has exactly 5 lanes {draft,grilled,spiking,harvested,rejected} and ideas.go LINE 49-51 documents StatusDraft as "The entry state" of a freshly-captured idea. There IS NO "proposed" status. firewall.ViaIdea produces StatusDraft. So "idée draft Status=proposed" → the entry state of a freshly-proposed idea = draft. Correct by-design, documented as OQ in both docs pages. PASS — verified the enum myself, not a weakening substitution.

**How to apply (a functional capability step, contrast CE02 ADR/decision step)**: (1) red-first fixture mirror compound_fixture_test.go pins state→cmd→events (1 proc + 1 draft + WroteKernel false) + non-green no-op; rapid repro mirror compound_property_test.go pins determinism + wall-across-input-space + non-green no-op. (2) determinism-first: Compound is pure (no DB/clock/rng), WHAT to capitalise = declared CE02 table not LLM, repro mirror both planes (rapid + fast-check in lib/compound.test.ts CE03 describe). (3) ui-completeness ACTION-path: GesturePanel.tsx on /compound runs the SAME TS twin (compound(GOAL_CLOSE)) — button run-compound renders procedural-kind=procedural + behavior-status=draft + no-kernel-badge data-wrote-kernel=false; VACUOUS on write-path (panel writes no truth, candidate is propose-draft never direct kernel write). (4) wall holds trivially: package returns VALUES only — grep INSERT/UPDATE/DELETE/kernel./mirrors./fitness./sql/pgx in runtime/compound/ hits ONLY a CE02 rationale STRING in capitalisation.go (describing the forbidden frontier), no actual write. (5) Linear unauth (only authenticate exposed) = OQ-CE03-linear, NOT a failure §11. (6) memory.WriteInput + idea persistence deferred to S31/S27 runtime wiring = by-design fwd-dep OQ, gesture is value-returning.

**SCAR did NOT recur this time**: gofmt -l runtime/compound/ CLEAN (the comment-alignment slip that hit CE01/CE02/BA16/BA20/HR01 did not appear). Still ran it myself. tsc clean, vitest 17/17 (6 new CE03), e2e 3/3 (CE01+CE02+CE03) on :3000 (:3100/:3200 dead — [[project_playwright_port_targeting]]). i18n: 13 gesture.* keys under compound namespace, parity clean fr+en ([[feedback_i18n_keys_missing]] checked). mint validate success. docs 64bbac5 on origin/main, internals has 3 layers (Implémentation·Méta·Méta-méta). go build ./... clean. Verified-green, no fix needed.
