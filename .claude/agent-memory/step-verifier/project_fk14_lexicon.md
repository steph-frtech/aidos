---
name: project-fk14-lexicon
description: §FK14 Lexicon Kernel + inter-layer linter verification (after FK13) — verified green zero corrections
metadata:
  type: project
---

§FK14 (after FK13, FKE-21) LEXICON KERNEL + linter inter-couches back/kernel/lexicon PURE write-NOTHING. LexiconKernel{Concept, Symbols map[Layer]string} over 16 CLOSED layers (human/bdd/code/test/db/api/event/log/metric/mcp/skill/agent/doc/ci/policy/memory). Lint(k,[]Observation)→[]Drift PURE/TOTAL/DETERMINISTIC set-membership string-equality (no LLM): 3 closed DriftKind RENAMED(symbol≠pinned — FK14 fault-injection rename-table-out-of-lexicon→red) / UNKNOWN_SYMBOL(layer known but lexicon pins nothing — NOT free pass) / UNKNOWN_LAYER(outside 16); drifts sorted stably (layerIndex canonical, then Symbol). Clean=len(Lint)==0. Validate refuses NO_CONCEPT/UNKNOWN_LAYER/EMPTY_SYMBOL (subset of 16 OK — pure fn carries human+code, endpoint more). STORAGE FORK TRANCHÉ ICI (ADR 0044): lexicon rides inside content-addressed kernel.link body link_kind:"lexicon" NOT new record-kind (same as WhyTree FK13/caused_by FK12); SerializeBody emits symbols in closed Layers() order→map-order-independent address; Record reuses records.NewRecord(KindLink)→id==version==Hash; renamed symbol⇒new version (KRD §12). DONE-CRIT BOTH PROVEN: (1) fault-injection 3 planes Go TestLint_RenameTableOutOfLexicon_IsRed + MCP TestLint_RenamedTable + Playwright "renamed table is RED" 1 RENAMED drift; (2) property TestProperty_Lint_DriftExplainedByLexiconAndSymbols(rapid, every drift explained ONLY by inputs)+Deterministic+CleanIffNoDrift+StableOrder+RecordRoundTrip + TS twin fast-check. gofmt CLEAN(NO scar this time) vet exit0 broad-build exit0 property -count=2 -rapid.checks=3000 0.383s WALL grep CLEAN(no INSERT/UPDATE/Exec/pgx); migration kernel_lexicon_baseline.sql DOC-ONLY additive CREATE-IF-NOT-EXISTS+GRANT SELECT+REVOKE writes+idempotent link_kind index. MCP aidos-lexicon-linter 4 PURE write-nothing lint/validate/serialize/layers main_test 7. TS twin lib/lexicon.ts Go-authoritative same 16 layers+3 drift kinds+stable order vitest 11/11 tsc clean(only pre-existing behavior-capture:101 'Kind') biome 6 clean. /lexicon LexiconPanel useState pick-scenario→lint→render drifts READ-ONLY (lib/lexicon-data.ts 4 fixtures RETURN_REQUEST); e2e tests/e2e/lexicon.spec.ts 4/4 GREEN live:3170 3.4s scoped caseSelect by aria-label /scénario|scenario/i (RECURRING scar handled correctly: pickLabel="Choisir un scénario"/"Pick a scenario" matches; header Projet combobox avoided). i18n fr5396==en5396 lexicon 23==23 nav.lexicon WorkbenchHeader:113. next-build ƒ /lexicon. docs 3-layer Impl/Méta/Méta-méta docs.json:481-482 HEAD a059f50==origin/main pushed. OQ Linear-unauth/Mintlify-reindex-lag/pre-existing-behavior-capture-TS-error by-design. verified-green ZERO corrections.
