---
name: wb2-05-grille
description: WB2-05 grid niveau×facette screen — verified GREEN zero corrections; the two axes, Σ conservation invariant
metadata:
  type: project
---

§WB2-05 (after WB2-04, spec WB2_PLAN.md) /v2/grille = GRILLE niveau × facette (FKE-1.4 « les deux axes »): matrice 7×8 (GRID_LEVELS=SOURCE_ORDER les 7 rungs product→entity, GRID_FACETS=FACET_LETTERS F·I·S·B·R·V·M·X), table cliquable read-only; clic cellule→détail (count + liste specs)→chaque spec→/v2/anatomie/[id] (anatomy real=WB2-06). TWIN lib/v2/grid.ts PURE buildGrid/validateGrid/cellKernels: sommes Σ COMPTÉES (rowTotals/colTotals/total), invariant cardinal Σ rows=Σ cols=total=#kernels, placement déterministe (buckets[levelIdx][facetIdx]), ids triés lexico, rejet coordonnées hors jeu clos (GridError level_unknown/facet_unknown/empty_id). REUSES SOURCE_ORDER (besoin-grammar), FACET_LETTERS+isFacetLetter (lib/v2/idea→lib/facets), KernelNode+syntheticComposes(240) (lib/v2/kernel-tree WB2-04) — la grille est une AUTRE lecture des mêmes 240 kernels. La transversale invariant/policy n'a PAS de ligne (grille des 7 rungs only).

DONE-CRIT ALL MET: twin (sommes déterministes Σ=total) + property GREEN (vitest 8/8: 7×8, déterminisme même-entrée→même-sérialisation, CONSERVATION Σ=total=nodes.length, COUVERTURE chaque kernel exactement 1 cellule aucune perdue/dupliquée seen.sort===ids.sort, count===kernelIds.length, ids ordonnés stables, cellKernels vide→[] jamais lien mort, rejet level/facet/empty + invariant→level_unknown, syntheticComposes(240)→total=240); e2e (clic cellule→specs, sommes cohérentes) — Playwright 2 tests: 7×8 + Σ rows=Σ cols=grandtotal=240 lus au DOM + clic product×F→liste = exactement count specs + clic spec→/v2/anatomie/<id> v2-anatomie-kernel-id===id + anti-write writes===[] ; coexistence /v2/grille 200 ∧ /v2/verticale rend grille (concept-title "Verticale") ∧ / 200.

ÉCRAN: GrilleScreen (Server, getTranslations v2Grille + getLocale, concept=entry("verticale") préserve contrat WB2-02 v2-concept-title/def) rendu à 2 routes /v2/grille (étape) ET /v2/verticale (slug glossaire) deux segments statiques priment sur /v2/[slug]; GridClient (client useMemo syntheticComposes(240)→buildGrid→grid, useState selected→clic cellule→détail). KEYS passées à GridClient: total/levelAxis/facetAxis/specsHere/emptyCell; GrilleScreen utilise direct title/eyebrow/subtitle/wallNote → 9 keys v2Grille fr==en parity.

VERIFIED-GREEN ZERO corrections: vitest grid 8/8, lib/v2 ALL 55/55 (6 files, 47→55 +8 du nouveau twin, prior-green intact), tsc EXIT0 (rm -rf .next/dev d'abord — SAME stale validator.ts scar que WB2-04), biome clean 6 web-files (e2e dir biome-ignored), next build "Compiled 13.1s" /v2/grille+/v2/verticale ƒ. WALL clean (grep grid.ts+grille/+verticale/ : NO fetch/POST/PUT/PATCH/DELETE/kernel/mirrors/fitness/propose/ChangeSet — projection lecture pure). Determinism-first: buildGrid authoritative pure code, Σ comptées not estimées, no LLM/clock/rng, fast-check repro mirror. Docs 3-layer (Implémentation·Méta·Méta-méta) docs.json:499-500 mint validate PASS HEAD a3d1b8c==origin/main pushed.

SSR (like WB2-04 — full skeleton SSRs): curl :3406 /v2/grille rend les 56 cellules (7×8) + 8 colhead + 7 rowhead + 8 coltotal + 7 rowtotal + grandtotal=240 + grid + title + wall-note tous SSR'd; /v2/verticale SSRs concept-title="Verticale" + grid; /v2/anatomie/k0 SSRs kernel-id="k0". cell-detail/specs rendus client-side après clic (useState selected). SCAR-RESPECTED next start -p 3406 (40s timeout self-kill + lsof -ti kill exact pid), prod :3000 re-200 (NEVER pkill next).

OQ by-design (no residual): syntheticComposes(240) SYNTHETIC til kernel store expose composes réels / anatomie real=WB2-06 / Linear MCP unauth (only authenticate exposed) / Mintlify search-index propagation lag. Executor report fully accurate.
