---
name: fk09-conscience
description: §FK09 (after FK08, FKE-6.3) la CONSCIENCE = PURE deterministic AGGREGATOR Reconcile(Input)→ConsciousnessReport composing EXISTING judges' sourced verdicts + FKE-31 decision cards; NO new judge (§8); X stays advisory; verified-green after 1 RECURRING gofmt fix
metadata:
  type: project
---

§FK09 (ROADMAP-fke, after FK08, FKE-6.3) the CONSCIENCE — back/runtime/conscience PURE write-NOTHING. Reconcile(Input)→ConsciousnessReport is a PURE/TOTAL/DETERMINISTIC AGGREGATOR composing the verdicts of the EXISTING judges into ONE report per kernel + §FKE-31 decision cards. THE LOAD-BEARING RULE (FKE-6.3): the conscience is an AGGREGATOR, NOT A NEW JUDGE — an active evaluator-organ would be "the second agent that validates" the Tome refuses as proof (§8). So Reconcile NEVER re-judges: it READS sourced verdicts (COPIES Verdict verbatim) and ROUTES them; every report line carries its Source judge.

ANTI-FABRICATION: knownSources CLOSED set (runner/completeness/facet/semantic_diff/reality_mirror/sensor/ledger) — a verdict from an unknown Source is DROPPED (the conscience invents nothing). The conscience is NEVER itself a Source.

COMPOSITION 2 paths: (1) reads FK08 facetwire.SkeletonReport DIRECTLY — each ColumnReport col → if soft uses col.Advisories else col.Divergences; empty→1 green facet pair "skeleton"; each div→PairVerdict pair=d.Rung verdict=red(hard)/advisory(soft) drift=facetDrift(facet) detail=d.Kind. (2) extra SourcedVerdicts copied verbatim, unknown dropped, red-on-soft-X DOWNGRADED to advisory (§13.6 X never clicks hard).

VERDICT red("drift") iff any HARD pair red; SOFT X NEVER flips (stays "aligned" w/ advisory cards §13.6). sortPairs canonical (sourceOrder-rank, facet, pair, verdict) stable→byte-identical under input reorder. DECISION CARD per divergence (red→blocking + advisory→advisory card): content-addr ID=DC-+records.Hash(canonical gap kernel/source/facet/pair/drift)[:12] idempotent same-gap→same-ID never seq/clock; routeOptions §FKE-30→§FKE-31 PURE table (security→OptBlock default; advisory→OptKeepExperimental; contract/semantic→OptAskUserDecision; evidence/test/incompleteness→OptFixBelowWall). sortCards by ID. Report Bytes/Hash=records.Canonicalize+Hash. Aligned kernel→0 cards.

DONE-CRIT ALL PROVEN: property mêmes-verdicts-entrée→même-rapport (TestProp_Reconcile_Deterministic byte/hash-id + InvariantUnderInputOrder shuffle-invariant) / rapport=verdicts-sourcés-seulement (TestProp_ReportIsSourcedOnly + fixture OnlySourcedVerdicts made_up_judge dropped) / e2e divergence→decision-card-actionnable (Playwright 'a divergence produces its actionable decision card' + fixture/vitest). Extra props XNeverDrifts + DivergenceProducesCard(1-per-divergence). fixtures: AllAlignedNoCard / FacetBreakReddensFromSkeleton-security-card-OptBlock / XAdvisoryNeverBlocks / SourcedRedOnXDowngradedToAdvisory / EmptyKernelAligned / CardIDContentAddressed.

SENSORS: gofmt FIXED 1 RECURRING scar (conscience.go const-block alignment DriftKind + doc-comment list-continuation reflow — gofmt -w mechanical, non-blocking) then CLEAN; vet clean; PROPERTY -count=2 -rapid.checks=3000 8.058s; broad-build ./... exit0; prior-green facetwire/facets/records intact; WALL grep CLEAN (no INSERT/Exec/pgx/db — package imports only facets/facetwire/records/json/sort PURE no-LLM no-clock no-rng).

MCP aidos-conscience 2 PURE write-nothing tools reconcile (full report) + decision_cards (cards only) main_test 5/5 both tools. TS twin lib/conscience.ts Go-authoritative card-ID FNV-1a-display-only (Go SHA256 authoritative, consistent prior steps) reuses lib/facetwire isSoft/SkeletonReport, CARRIES OWN reproducibility property vitest 5/5; tsc CLEAN (zero non-preexisting errors, behavior-capture pre-existing not present in filter); biome 6 files clean. /conscience action-capable useActionState reconcileAction→reconcile twin writesTruth-ABSENT (pure projection correct, card-acting goes idea→mirror→/goal); fixtures.ts SEPARATE module 4 scenarios (aligned/runner-drift/break-security/break-experience) reuse facetwire wireSkeleton, scenario-ids+testids match e2e (scenario-select/reconcile-submit/verdict-badge[data-verdict]/pair[data-source,data-verdict]/decision-card[data-facet,data-advisory]/card-option/card-blast/no-cards/determinism-badge). next-build ƒ /conscience present. Playwright 5/5 RAN GREEN live:3000 35.5s (projects.project DB-snapshot warnings = unrelated /projects route noise). i18n fr5260==en5260 conscience block both + conscienceNav nav WorkbenchHeader:206. docs 3-layer Impl:9/Méta:25/Méta-méta:37 docs.json:471-472 mint-validate PASS HEAD fc5ce34==origin/main clean-tree.

OQ by-design: Linear-unauth (only github+task MCP surfaced, can't move step issue); Mintlify-reindex-lag (pushed+validated, surfaces next crawl); live-verdict-stream wiring of judges into Reconcile owned by FK11 cockpit (FK09 provides the sourced-verdict input contract, mocks-what-doesn't-exist-yet bootstrap exception).

VERDICT: verified-green AFTER 1 correction (RECURRING gofmt const-block-alignment scar, same pattern as S104/S109/S112/S116 comment-align — executor leaves gofmt-dirty struct/const continuation lines, gofmt -w mechanical non-blocking fix).
