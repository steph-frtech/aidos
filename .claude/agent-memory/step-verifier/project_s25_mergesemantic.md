---
name: s25-mergesemantic
description: S25 semantic merge — pure MergeSemantic(base,left,right,heads)→MergeResult{clean|conflict|unresolvable} delegating verdict to S23 phases.IsStable; the mirror decides, not the text diff; read-only /semantic-merge; verified-green
metadata:
  type: project
---

S25 — semantic merge (KRD §122/§130, no_merge_without_semantic_green §82.2). Done criterion: merge resolves two branches by re-running mirrors on the MERGED CUT (not a text diff); a conflict = a mirror that cannot be simultaneously green; proven by fixtures. THE pinned case: a textually-clean refund EU/US pair (disjoint deltas — git auto-merges) with a red merged-cut mirror ⇒ conflict, BLOCKED, requires_authority.

- **Core** `back/archive/merge/merge.go`: pure total `MergeSemantic(base, left, right, heads) → MergeResult`. Assembles UNION SELECTION (base cut + left deltas + right deltas, last-writer left-before-right), concatenates links, de-dups sensors by id keeping LAST in (base,left,right) order, then hands the merged cut to S23 `phases.IsStable` — NEVER re-implements aggregate(S18)/propagation(S19)/red-wave(S22). Mappability gate: left.Ancestor==right.Ancestor==base.ID else `unresolvable`+OpenQuestion (never fabricated clean). merged_cut@hash via phase.Version() (S02 records.Hash, KindPhase body). Closed status {clean,conflict,unresolvable}. No I/O/clock/rng, never panics, never writes.
- **Mirrors**: fixture (5: done-criterion refund EU/US clean-text-red-mirror⇒conflict; disjoint-line cart same emergent invariant⇒conflict; free-space promo/help⇒clean; no-common-ancestor⇒unresolvable+OpenQ; base-with-itself⇒clean no-op) + rapid property (8: determinism+total, any-red⇒conflict, all-green⇒clean, identity⇒clean, text-never-overrides-red, conflict⇒authority∧named, unresolvable⇒no-fabricated-clean, never-panics). Front fast-check twin lib/semantic-merge.test.ts 6/6.
- **Front lib** `lib/semantic-merge.ts`: declared twin of Go MergeSemantic. NOTE: front oracle is SENSORS-ONLY (the UI data has no links) — faithful because the canonical triples carry no links; merged_cut@hash is a readable local digest (FNV-ish) mirroring SHAPE of Go records.Hash body (authoritative hash = Go, surfaced when wired = OQ).
- **UI** `/semantic-merge`: action-capable panel (pick triple + merge button → runs mergeSemantic, renders status badge/conflicting_mirrors/sentence/merged_cut@hash/requires_authority). READ-ONLY against truth (clean merge records via S20 ChangeSet) ⇒ ui-completeness vacuous on write-path. Nav wired in WorkbenchHeader. i18n semanticMerge.* (28 keys) + nav.semanticMerge present BOTH fr+en. FR badges "conflit"/"propre", overrideAuthority contains "humain", sentences contain "git" — all match e2e assertions.
- **Verified green**: gofmt/vet clean; go test ./archive/merge PASS + full ./archive/... PASS (prior green intact); go build ./... clean; vitest 6/6; biome clean 4 files; Playwright 4/4 on :3000 (PLAYWRIGHT_WEB_PORT=3000). Docs 2 pages (3 layers) registered docs.json, mint validate passes, pushed steph-frtech/docs main (9dc536c, HEAD==origin/main). Skill merge-semantic present. Linear AID-8 Done (note: AID-8=S25, AID-5=S24 — id offset persists).
- OpenQuestions (by-design forward deps, non-blocking): merged_cut@hash front digest vs authoritative Go hash (wired when /merge-semantic hits backend SELECT-only role); mirror records to mirrors schema back-filled at S06; clean-merge node INSERT rides S20 ChangeSet; no `aidos merge` CLI verb (later); no QD curation (S26).
