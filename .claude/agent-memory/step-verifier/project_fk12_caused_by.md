---
name: fk12-caused-by
description: §FK12 verification — the 7th versioned link kind caused_by + deterministic inverse red-wave trace
metadata:
  type: project
---

§FK12 (after FK11, FKE-35.1) the SEVENTH versioned link kind `caused_by` back/kernel/causedby PURE write-NOTHING; the BACKWARD causal edge = inverse of forward red-wave impacts (S22 §42), substrate the WhyTree (FK13) builds on; ADDITIVE to the six S17 §41 kinds (projects_to/derives_from/contracts_with/triggers/binds/mirrors) — closed set grows by 1, never edits them (anti-overwrite §9, ROADMAP's loose 'composes/contracts_with/impacts' phrasing reconciled to actual closed §41 set by executor — correct).

CORE: const Kind links.Kind="caused_by"; Edge{From:symptom,To:cause} both PINNED id@version reuses links.Ref.IsPinned; Validate PURE refuses unpinned-from(ErrUnpinnedFrom)/unpinned-to(ErrUnpinnedTo monster)/self-edge(ErrSelfCause From.ID==To.ID); Trace(symptom,edges)→CauseChain = PURE deterministic UPWARD BFS: build adj skipping malformed edges (TOTAL), cycle-detect FIRST via white/grey/black DFS over REACHABLE subgraph → ErrCycle (never partial chain, cycle elsewhere unreachable does NOT poison), then BFS records each node at SHORTEST hop dist, sort canonical (dist asc, id asc), symptom excluded from causes; SerializeEdgeBody/ParseEdgeBody round-trip content-addr via records.NewRecord(KindLink) (changed cause version ⇒ new version never mutation); 3 done-crit ALL PROVEN property: round-trip-versioned (alt cause version → different record.Version) + TraceDeterministic (same graph+symptom → byte-id chain) + ChainReachableAndOrdered (vs independent DFS+reachableSet oracle, only acyclic) + CycleRefused (vs hasCycleFrom DFS oracle) + ValidatePinnedNoSelf; fixture = §17 worked example checkout-accept←createOrder←{Order,authzPolicy}←add_total_col ordered nearest-first (dist1 createOrder, dist2 Order<authzPolicy by id, dist3 add_total_col) + leaf-no-cause + cycle-refused + self/unpinned-refused.

SENSORS: gofmt CLEAN (NO scar this step) vet clean test cached-ok PROPERTY -count=2 -rapid.checks=3000 0.268s broad-build exit0 WALL grep CLEAN (only comment hit re GRANT). MCP aidos-caused-by 4 PURE write-nothing tools validate/trace/serialize/kinds stdio main_test green. Migration kernel_causedby_baseline.sql additive/doc-only (reuses kernel.link with link_kind in JSONB, no new table, GRANT restated). TS twin lib/caused-by.ts Go-authoritative same Validate/trace/cycle/round-trip vitest 11/11 tsc CLEAN (only pre-existing behavior-capture:101 filtered) biome exit0 (3 noNonNullAssertion WARNINGS in caused-by.test.ts oracle helpers — NON-blocking, test-only). i18n fr5341==en5341 fr-only=[] en-only=[] causedBy 19==19 nav WorkbenchHeader:111 {href:/caused-by,k:causedBy}. /caused-by action-capable useState pick-graph→TRACE renders ordered chain/empty/cycle-refused + first-edge content-addr body, READ-ONLY writesTruth-ABSENT (pure projection correct). h1 t("title")="Lien caused_by" matches e2e /caused_by/i. next-build ƒ /caused-by EXIT0. docs 3-layer Impl:9/Méta:29/Méta-méta:43 docs.json:477-478 mint-PASS docs HEAD 040e257==origin/main clean.

VERIFIED-GREEN AFTER 1 CORRECTION: e2e tests/e2e/caused-by.spec.ts had ORDERING FLAKE — `page.getByRole("combobox")` AMBIGUOUS (TWO comboboxes: header "Projet" selector e12 + panel case-picker), `labelFor` iterated ALL `option` incl project options; cyclic test (runs 3rd in same worker) raced → "no option matching /cycl/i" (passed in isolation, failed in suite). FIXED: scoped caseSelect = getByRole("combobox",{name:/graphe causal|causal graph/i}) (panel aria-label "Choisir un graphe causal") + labelFor scopes options to that select. After fix 6/6 GREEN (3 tests × repeat-each=2, 2 workers) on fresh prod build :3187, biome clean. NOTE prod :3000 serves STALE build (404 /caused-by) — deploy track rebuild+restart out of scope. NOTE playwright.config uses PLAYWRIGHT_WEB_PORT (NOT _BASE_URL) + reuseExistingServer:!CI — must start own `next start -p NNNN` + set PLAYWRIGHT_WEB_PORT=NNNN to target a fresh build, else it spins a dev server on 3000/reuses stale.

OQ by-design (NOT residual): Linear MCP unauth (only authenticate surfaced, needs OAuth+restart) — FK12 issue not moved; Mintlify search index periodic-crawl lag (pages pushed live 040e257, mint validate+broken-links clean = deterministic proof); freezing a caused_by row = idea→mirror→/goal (no GRANT) bootstrap.

RECURRING SCAR (NEW): e2e `getByRole("combobox")` is AMBIGUOUS on any Workbench panel — the header ALWAYS has the "Projet"/"Project" selector combobox. ALWAYS scope the panel's select by its aria-label (name filter). A test that passes in isolation but fails in-suite = ordering race from an ambiguous/global locator (here a `locator("option")` over the whole page). See [[fk11-ai-lab]] [[fk10-autonomy]] for the FKE piste.
