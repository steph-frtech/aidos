---
name: fk15-tool-projection
description: §FK15 NOW FULLY GREEN — part (a) tooling projections + part (b) Fiche Tech & Suite de Tests (FKE-20.1) both delivered & verified all planes; the retry shipped (b), the load-bearing zero-new-truth criterion now proven
metadata:
  type: project
---

§FK15 RETRY — PASSED (verified-green, zero corrections). The prior FAIL (part b missing) is RESOLVED.
back/runtime/generators/techspec: PURE write-NOTHING assembler TechKernel→{Fiche de Spécification Technique, Suite de Tests Techniques}, the TWO assembled projections (NOT rows, FKE-20.1 «DEUX PROJECTIONS pas deux lignes»). Fiche=Contrat(F5)+Modèle(F4)+OSI stack(L7→L4 networked-only, pure-fn has NO OSI section, ErrOSIOnPureFunction)+facet specs S1/B1/R1/V1/M1+linked ADRs; Suite=N4/N5 TestGroups+per-OSI tests+facet tests S3/B3/R3/M3, distinct from N0 acceptance, NEVER carries F5 contract spec (no double-typing). 4 CLOSED OSI layers·5 facets S/B/R/V/M·7 test kinds·2 projections, all enforced in CODE not DB-CHECK. LOAD-BEARING done-crit «zéro nouvelle vérité» PROVEN as code: AssembledRefs(extracted FROM rendered `(`ref`)` bytes via extractRefs pure-parse) == DeclaredRefs(k) set-equality (no fabrication ⊆, no loss ⊇) — TestProp_ZeroNewTruth(rapid)+fixture. Byte-identical: TestProp_Determinism+TestProp_InputOrderInvariant, total declLess order(Ref,Title,Body) fixes Ref-collision order-leak (SAME scar as FK06 op-name collision). Hash-protected footer AIDOS-TECHSPEC-SOURCE-HASH; DetectDrift→HAND_EDITED(fault-inject)/MISSING_MARKER/STALE_HASH pure byte+hash-eq. Storage FORK reuses kernel.link link_kind:"techspec" (SAME FK14/FK15a, no new record kind), Record=records.NewRecord id==version==Hash changed-source⇒new-version §12. gofmt CLEAN vet clean -rapid.checks=3000 4.4s broad-build exit0 prior-green generators/derivedoc/webcomponent/toolproject intact WALL grep CLEAN(migration DOC-ONLY idempotent CREATE-IF-NOT-EXISTS+SELECT-only, no INSERT/UPDATE). MCP aidos-tech-spec 6 PURE write-nothing(assemble/assemble_all/declared_refs/assembled_refs/detect_drift/validate) main_test green. TS twin lib/tech-spec.ts Go-authoritative(declaredRefs/assembledRefs/detectDrift) vitest 8/8 tsc clean(only pre-existing behavior-capture). /tech-spec action-capable: TechSpecPanel calls same pure assemble/detectDrift, RUN→renders file+source-hash+live zero-new-truth verdict(data-ok)+drift, READ-ONLY(wall, freeze via propose→/goal) e2e 5/5 LIVE :3148 3.7s (Fiche+OSI+zero-truth / Suite no-contract / Scorer no-OSI / clean-green / hand-edit RED HAND_EDITED). i18n fr5450==en5450 frOnly0 enOnly0 techSpec 28==28 nav.techSpec:61 WorkbenchHeader:141. docs 3-layer fk15-tech-spec docs.json:485-486 mint validate PASS HEAD 9d27633==origin/main clean. OQ by-design: Linear-unauth, Mintlify-reindex-lag.

PRIOR FAIL CONTEXT (resolved):
§FK15 (after FK14 lexicon, FKE-20/FKE-20.1) — PARTIAL: verifier FAILED the step.

PART (a) tooling projections — FULLY GREEN, verified all planes:
- back/kernel/toolproject PURE write-NOTHING; ToolingKernel{Project,Sources[]} 5 CLOSED kinds(policy/memory/style/architecture/agent-profile); Emit/EmitAll deterministic render + protectionFooter `<!-- AIDOS-TOOLING-SOURCE-HASH: %s ... -->`; SourceHash=SHA-256 of records.Canonicalize(SerializeBody); 4 CLOSED targets(CLAUDE.md=policy+arch+style / AGENTS.md=agent-profile only / .cursorrules=policy+style / memory-bank.md=memory) NO double-typing; DetectDrift→3 DriftKind HAND_EDITED(marker-ok bytes-differ=FK15 fault-inject)/MISSING_MARKER/STALE_HASH PURE byte-compare+hash-eq NO-LLM.
- STORAGE FORK same as FK14: kernel.link link_kind:"tooling" NOT new record-kind; SerializeBody emits sources in SourceKinds()-order+id-order (order-indep address); Record reuses records.NewRecord→id==version==Hash, changed source⇒new version §12; ParseBody round-trip lossless.
- DONE-CRIT (a)-subset MET: property TestProp_SameKernel_ByteIdentical(rapid)+OrderIndependent+DriftIffEdited+RoundTrip+SourceHashDrivesFiles; fixture 9(incl hand-edit→HAND_EDITED, stale-hash, missing-marker, legacy-content round-trip). gofmt CLEAN vet build exit0; kernel test -rapid.checks=2000 ok 1.3s WALL grep CLEAN; MCP aidos-tool-projection 5 PURE(emit/emit_all/detect_drift/validate/serialize) main_test ok; TS twin Go-authoritative(source-hash FNV-1a display-only, Go SHA-256 authoritative) vitest 10/10 tsc clean biome 6 clean; /tool-projection action-capable(emit+drift-check READ-ONLY correct) e2e 4/4 live:3137 3.1s combobox /target|cible/i UNAMBIGUOUS(this WorkbenchHeader version has NO Projet select); i18n fr5422==en5422 toolProjection 25==25 nav:140; docs 3-layer docs.json:483-484 HEAD 840d518==origin/main.

WHY FAILED — PART (b) MISSING and IN-SCOPE:
ROADMAP-fke.md:109-113 FK15 Objectif/Done-crit EXPLICITLY include (b) FKE-20.1: the **Fiche de Spécification Technique** (assembles Contrat F5 + Modèle F4 + facet specs S1/B1/R1/V1/M1 + ADRs, ranged by ISO layers) AND the **Suite de Tests Techniques** (N4/N5 + S3/B3/R3/M3). Done-crit: "property — mêmes kernels → mêmes fichiers byte-identiques (outillage ET fiche tech ET suite de tests) ; la fiche technique assemble exactement les déclarations techniques existantes (zéro nouvelle vérité)".
- Executor implemented ONLY (a), called (b) an "OpenQuestion/forward-scope". WRONG: (b) assembles declarations that ALREADY EXIST (verified: kernel/entities for F4/F5, kernel/mirror/facetwire+kernel/facets for facet specs, runtime/generators/derivedoc as assembled-projection precedent). NOT substrate owned by a later step ⇒ NOT a §6 bootstrap forward-dependency ⇒ it is a BLOCKING residual against the step's OWN done-criteria.
- NOTE: the workflow-supplied done-criteria string was the (a)-only SUBSET ("property byte-identical; hand-edit détecté; contenu kernelisé sans perte") — but ROADMAP is the step-scope source of truth and names (b). When workflow done-crit looks narrower than ROADMAP, CHECK ROADMAP-fke.md for the full step objective.

TRAP/SCAR confirmed: `go test ... -rapid.checks=N` on a pkg WITHOUT rapid (the MCP pkg) FAILS with flag-usage dump (`FAIL ... 0.004s`)—NOT a real failure; test MCP pkg separately without the flag (recurring across FK steps).

OQ by-design: Linear-unauth (could not flip FK15 issue back to In Progress, record OQ), Mintlify-reindex-lag.
