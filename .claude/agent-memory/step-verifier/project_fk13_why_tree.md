---
name: fk13-why-tree
description: §FK13 verification — the WhyTree / geste /why (the 5-whys redressed) built on FK12 caused_by
metadata:
  type: project
---

§FK13 (after FK12, FKE-35.1) the WhyTree = the 5-whys REDRESSED, geste `/why` (extension of /diagnose+/learn) back/kernel/whytree PURE write-NOTHING; builds on FK12 caused_by [[fk12-caused-by]] (REUSES causedby.Trace verbatim for the upward walk). Build(Input)→WhyTree PURE/TOTAL/DETERMINISTIC no DB/clock/rng/I/O/LLM (the judge is the reproduction bool, never a prompt — determinism-first §6/§8).

CORE 5-step Build: (1) Validate provenance closed set {incident,mirror,human} else ErrUnknownProvenance; (2) causedby.Trace upward (cycle-refusing ⇒ ErrCycle propagated verbatim NEVER partial tree); (3) each candidate cause on trace REQUIRES positive Reproduction{CauseID,Reproduced bool,Detail prose-advisory} else ErrCauseNotReproduced (ANTI-CONFABULATION — missing OR negative repro both refused, NOT silently dropped); (4) root = deepest reproduced node (leaf symptom = its OWN root, IsLeaf()); (5) Terminal mirror OBLIGATORY MirrorID!="" else ErrNoTerminalMirror(WHYTREE_NO_MIRROR) AND must reflect root else ErrTerminalMismatch. deepest() = max Depth ties by id desc; traceDepths() recomputes BFS hop-dist REUSING causedby.Validate to skip malformed identically (no drift). SerializeBody/ParseBody/Record content-addr via records.NewRecord(KindLink) link_kind="why_tree" discriminator (8th-ish link namespacing beside FK12 caused_by) — changed cause/mirror version ⇒ NEW record version never mutation §12; SerializeBody sorts causes defensively (Depth asc,id asc) so address order-independent.

DONE-CRIT ALL 3 PROVEN: fixture (9 funcs) worked example checkout-accept←createOrder←{Order,authzPolicy}←add_total_col root=add_total_col + WHYTREE_NO_MIRROR refusal + non-reproduced-refused + missing-repro-refused + leaf=own-root + terminal-must-reflect-root + cycle-refused + unknown-provenance + round-trip-content-addr(changed terminal⇒new version); property (rapid 7 invariants) BuildIsDeterministic(same input→byte-id tree/same err)+ReproductionGate(drop any reachable cause's repro→ErrCauseNotReproduced)+AllAdmittedCausesReproduced+NoMirrorRefused+RoundTripContentAddressed+cycle+total. e2e (4/4) incident→tree→root add_total_col→terminal mirror+red-wave / WHYTREE_NO_MIRROR / WHYTREE_CAUSE_NOT_REPRODUCED / CAUSED_BY_CYCLE no-partial-tree.

SENSORS: gofmt 1 FIX (RECURRING SAME scar — whytree.go Build doc-comment numbered-list `//\t1.` tab-indent → gofmt rewrites to `//  1.` 2-space + continuation lines; executor's go test was likely CACHED so missed it). After fix gofmt CLEAN. vet exit0 broad-build(go build ./...) exit0 PROPERTY -rapid.checks=2000 0.137s prior-green causedby intact WALL grep CLEAN (INSERT/UPDATE/pgx/Exec none). NOTE test-runner trap: `go test ./mcp/why-tree/... -rapid.checks=2000` FAILS with flag-not-defined (mcp pkg has NO rapid import) — run rapid flag ONLY against kernel/whytree, mcp separately; NOT a real failure.

MCP aidos-why-tree (back/mcp/why-tree) 3 PURE write-nothing tools build/serialize/kinds stdio; classify() maps Err*→closed codes WHYTREE_NO_MIRROR/WHYTREE_CAUSE_NOT_REPRODUCED/WHYTREE_TERMINAL_MISMATCH/CAUSED_BY_CYCLE/UNKNOWN_PROVENANCE; main_test green. Migration kernel_whytree_baseline.sql additive/doc-only (no new table, reuses kernel.link link_kind in JSONB, GRANT restated). TS twin lib/why-tree.ts REUSES caused-by trace, mirrors Build/deepest/traceDepths/serializeBody faithfully Go-authoritative; reproducibility mirror why-tree.test.ts fast-check vitest 12/12; tsc CLEAN (only pre-existing behavior-capture); biome 6 files clean. i18n FULL parity fr5372==en5372 fr-only=[] en-only=[] whyTree 30==30 nav whyTree both present WorkbenchHeader:112 {href:/why-tree,k:whyTree}. /why-tree action-capable useState pick scenario→build (same pure fn)→renders ordered reproduced causes(data-order/data-reproduced)+root-cause+terminal mirror+red-wave+content-addr body OR refused(data-error); READ-ONLY writesTruth-ABSENT (freezing terminal goes propose→/learn→/goal). next-build ƒ /why-tree exit0. docs 3-layer steps/concept+steps/internals/fk13-why-tree.mdx docs.json:479-480 mint validate PASS .aidos-docs HEAD f0ed39f==origin/main SYNCED.

OQ by-design (NOT residual): Linear MCP unauth (only authenticate surfaced, needs OAuth+restart) — FK13 issue not moved; Mintlify live-index lag (FK13 just-pushed not yet crawled, FK12 IS — pages live f0ed39f, mint validate clean = deterministic proof); freezing terminal mirror = idea→mirror→/learn→/goal (no GRANT) bootstrap; /projects snapshot DB noise from header pre-existing unrelated.

VERIFIED-GREEN AFTER 1 CORRECTION (gofmt fix on whytree.go). RECURRING SCAR CONFIRMED AGAIN: Go doc-comment numbered/struct-tag continuation-line gofmt scar (same S104/S109/S112/S116/FK09 family) — executor's go test CACHED → missed it. ALWAYS run `gofmt -l <pkg>` fresh during verification, never trust executor's cached `go test`. e2e ran reusing my own `next start -p 3113` WITHOUT CI env (reuseExistingServer:!CI — setting CI=1 makes Playwright spawn its OWN server on the port → 'already used' conflict; run withOUT CI to reuse). See [[fk12-caused-by]] for the combobox-aria-label scar (handled correctly here by executor).
