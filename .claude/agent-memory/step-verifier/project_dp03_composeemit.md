---
name: dp03-composeemit
description: DP03 docker-compose emitter — verified green ZERO corrections; executor pre-suppressed the noTemplateCurlyInString scar (first DP step clean on biome without verifier fix)
metadata:
  type: project
---

DP03 = additive Target `docker-compose` (EPIC A, after [[dp02-stackmanifest]]). Done-criteria ALL MET, verified live by me, ZERO corrections.

**Shape:** back/runtime/composeemit — pure emitter, OWN targetOrder (honoemit/apisurface pattern: each emitter package owns its targets, global matrix = union; generators S34 untouched, commit is 100% additive +1750/-0 incl. 1-line nav entry). Emit = Validate(DP02, refusal→BlockReason CodeOutOfScope w/ how_to_fix) → HashManifest (S02 source_hash) → render (sorted services/volumes, "\n", protected header `# CODE GENERATED BY AIDOS — DO NOT EDIT. source: <hash>`) → records.Hash output_hash, path back/gen/<app>/docker-compose.yml, Drifted = generators.Drifted verbatim. 9 rapid laws (repro ∀×2 + Example ×100, server HTTPS router websecure/tls/certresolver + ${APP_NAME}-https-redirect middleware, volume bind device ${DEVICE_VAR} type:none o:bind, external:true unless network owns, NO ports: key, refusal closed codes, 1-byte drift, double content-address + header + below-the-line path + target registered, container_name/env_file/restart conventions). TS twin lib/stack-emit.ts pins GO_OUTPUT_HASH efea2247…6fea24c1 + GO_SOURCE_HASH 8011728e…62acdd2 (== DP02 manifest hash — emitter reuses kernel address). Empty image → ${<SERVICE>_IMAGE} env-var ref (OQ-DP03-emitted-image).

**Verified live:** go test composeemit+stackmanifest ok, gofmt/vet clean, no time.Now/rand/Getenv; vitest 2080/2080 (baseline 2070 intact); tsc 0; biome CLEAN on all changed files; build → next start :3210 → 20 passed (stack-emit 4 + v3 16), :3210 killed, prod :3000 200. i18n stackEmit fr22==en22 + nav.stackEmit both. No hardcoded URL/secret (greps clean), no Temporal/Coolify/Drizzle, no parasite binaries, no untracked source. Docs 2 pages 3 layers, docs.json:565-566, mint validate+broken-links clean, 691f2bd==origin/main, live 200/200. Code 337d845==origin/build/s00-s47 (0/0). Wall: package imports no DB at all; screen = controlled textarea + server action pure measure, zero fetch/SQL.

**SCAR BROKEN:** the DP01/DP02 noTemplateCurlyInString scar did NOT recur — executor pre-suppressed with biome-ignore-start/end ranges (lib) + single-line ignores (e2e spec) with reasons. First DP step biome-clean without a verifier fix. Still always re-run biome on DP emitters.

**Report inaccuracy (non-blocking):** notes claim the 2 user requirements are "déjà câblées côté v3" — actually `validation_humaine` still has ZERO hits anywhere in front/web (vacuously non-regressed, same as DP01/DP02); only the dev-URL exists (infra/dev-preview, 139e224, untouched). Keep re-checking both each DP step; v3 spec stays tests/e2e/v3.spec.ts 16 tests.

**Re-verified 2026-06-13 (HEAD 50d233e, post-DP10):** workflow re-dispatched DP03; executor correctly recognized it done and re-checked without redoing. I re-ran EVERYTHING live: go test -count=1 composeemit+kernel/stackmanifest ok (note: stackmanifest lives in back/KERNEL/ not runtime/); biome 7 files truly clean (no latent DP01-style scar — composeemit.go/twin/twin-test/e2e UNCHANGED since 337d845, only the /stack-emit UI extended additively by DP04/DP05 — already verified there); vitest 2162/2162; e2e 20/20 one run :3210 (stack-emit 4 + v3 16), :3210 freed, prod :3000 200; mint validate success (mint NOT on PATH in verifier shell → use `npx --yes mint validate`), docs HEAD==origin/main, pages live 200/200; validation_humaine STILL 0 hits, infra/dev-preview intact. ZERO corrections again. Pattern: a re-dispatched already-done step = full live re-run, latent scars can only hide in files later commits touched — diff 337d845..HEAD scoped the risk to the UI files.

**Re-verified AGAIN 2026-06-13 (HEAD fbd23c5, post-DP11):** 3rd full live re-run, ZERO corrections. go test composeemit+kernel/stackmanifest ok, gofmt/vet clean, no stray binaries; throwaway test re-derived live Go hashes SOURCE=8011728e…62acdd2 OUTPUT=efea2247…6fea24c1 PATH=back/gen/alphashop/docker-compose.yml == e2e pins (stack-emit.spec.ts:22-25) EXACTLY + re-emit byte-identical (cleaned up the throwaway after); vitest twin 10/10 + full suite 2169/2169 (216 files); v3 e2e 16/16 + stack-emit 6/6 one run PORT=3210 fresh server (NOT prod); mint validate success + broken-links clean (npx --yes mint), docs HEAD 62d9dbe==origin/main, docs.json:565-566, internals 3 layers present; no hardcoded URL/secret, no Temporal/Coolify/Drizzle (only forbid-comment hit), wall migration SELECT-only + REVOKE writes, front actions write nothing. **Report inaccuracy (non-blocking):** report cites commit 691f2bd for code — WRONG, actual DP03 code commit is 337d845, 62d9dbe is the docs HEAD; harmless. validation_humaine STILL 0 hits. e2e WebServer noise `projects.project does not exist` = unrelated /projects route Postgres forward-dep (S17/S31), NOT DP03.

**OQ (by-design, never residual):** OQ-DP03-emitted-image (registry ref at deploy, DP05+); per-service mount path not pinned by DP02 AST → volumes top-level only, no per-service volumes: key (DP05 tooth); Linear MCP unauth (recurring — only authenticate tool exposed); no migration needed (no new kind).
