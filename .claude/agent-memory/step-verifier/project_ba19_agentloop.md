---
name: ba19-agentloop
description: BA19 = agentloop MCP server — the capability DOOR over agentloop.Drive (BA15); 3 tools (drive gated at transport boundary / watch deterministic replay / list); the crucial transport-integration "is it real?" mirror mounts the real server over in-memory transport
metadata:
  type: project
---

BA19 = `back/mcp/agentloop` MCP server (one op = one MCP tool, ADR 0009): `agentloop_drive` (gated), `agentloop_watch` (deterministic replay), `agentloop_list`. The capability door over `agentloop.Drive` (BA15). Action source is a DECLARED `ScriptedGenerator` (no LLM on the path — the model is BA17's gated seam), so the SHELL + wall are proven on the real call path without a model.

- **`gate()` (shared by drive+watch, BEFORE Drive)**: (1) layer must exist+project (else AGENT_TOOL_NOT_BOUND); (2) BA18 `ActAsIdentity` must bind loop to EXACTLY the requested LayerRef (AGENT_IDENTITY_UNVERIFIED); (3) BA07 `ToolAllowed(impl, targetServer, targetTool)` default-deny — the TRANSPORT-BOUNDARY capacity check (gap C2). Refusal ⇒ Refused:true + BlockReason + NO run executed.
- **Transport-integration mirror** (`transport_integration_test.go`, gap C2 "is it real?"): mounts the REAL `newMCPServer` over `mcp.NewInMemoryTransports()` + real client; `TestTransport_BoundToolDrivesGreen` (door open ⇒ green run, no above-waterline authorised write) + `TestTransport_UnboundToolRefusedAtBoundary` (unboundView rebinds the only tool to a different server ⇒ refused over the wire with AGENT_TOOL_NOT_BOUND, no run). The enforcer is ON the call path, not a unit-test return.
- **3 scenarios (closed set):** happy (write app/checkout.go ⇒ mirror green ⇒ green), kernel-write (1st turn writes kernel/order ⇒ GateAction refuses in place AGENT_WRITE_ABOVE_WATERLINE, run continues + 2nd legal write closes), over-budget (tightBudget tokens=5 < 1M cost ⇒ abandoned). Journey N0 feature has all 4 scenarios incl watch-replay.
- **Repro mirror** (`repro_property_test.go`, rapid): same (layer_ref, scenario, item, timestamps) ⇒ byte-for-byte same AgentRun (mustJSON eq); watch timeline matches run; DriveNeverWritesTruth across all scenarios. Timestamps SUPPLIED (no arg-less clock).
- **Wall held STRUCTURALLY:** AgentRun type carries NO version/mirror field (unrepresentable); no INSERT/UPDATE/DELETE/GRANT in any agentloop .go (read-only over mocked SELECT view, persists nothing); kernel-write refused in-place via GateAction. `isAboveWaterline` local zone check (kernel/mirrors/fitness/back/kernel/back/migrations + prefix-seg).
- **Front twin** lib/agentrun.ts: `driveAgentloop` (toolAllowed boundary gate → drive scenario) + `agentloopWroteNoTruth` + AGENTLOOP_SCENARIOS/scenarioTurnsTs. tsc 0, vitest 126/126 (agentlayer.test.ts).
- **UI** action-capable /agents `agentloop-run` section IN AgentsPanel (NOT impl-viewer ⇒ viewer button-count guard stays 7, same SCAR pattern as [[project_ba18_identity]]/[[project_ba17_sandbox]]): « Lancer un run » → driveAgentloop, scenario selector + unbound fault checkbox, live timeline w/ per-action verdicts, no-truth badge, BlockReason+how_to_fix. 15 BA19 i18n keys (runDrive*/runScenario*/runUnbound*/runRefused*/runResult2/runNoTruth/runAllowed/runComputed) ALL in fr+en + ALL wired via t() in page.tsx — earlier exec-side mismatch (runResultLabel etc. predating BA19) was caught + resolved, cross-checked per [[feedback_i18n_keys_missing]].
- Evidence: go build+vet+gofmt clean; go test mcp/agentloop ok + full ./runtime/... ALL-OK (prior green intact); tsc 0, vitest 126/126, biome clean; e2e agents.spec 67/67 on :3000 (4 BA19) — :3100 stale 404, :3200 dead, see [[project_playwright_port_targeting]]; mint validate passed, internals 3 layers (Implémentation/Méta/Méta-méta), docs pushed 6149924 (HEAD==origin/main).
- Determinism-first: pure compositions over Drive, no LLM/clock/rng above supplied, repro mirrors both planes, capacity check = set-membership not judgment.
- OQ (fwd-dep, non-blocking): live LLM provider on the path = BA17 seam swap; Linear MCP unauth (only authenticate/complete exposed, no list/update) ⇒ BA19 issue status not flippable programmatically.
- verified-green.
