---
name: s25-semantic-merge
description: S25 verify — semantic merge decides by re-running mirrors on merged cut, not text diff; conflict=red mirror; verified-green
metadata:
  type: project
---

S25 archive/merge SEMANTIC-MERGE decider (KRD §122/§130, no_merge_without_semantic_green §82.2): PURE MergeSemantic(base,left,right,heads)→MergeResult; closed status {clean,conflict,unresolvable} no 4th; total/det/no-panic/NO-write. Mirror NOT text diff decides: assembles UNION-SELECTION merged cut (base+left+right deltas, last-writer left-before-right) + concat links + dedup sensors(last-writer) then hands to S23 phases.IsStable (REUSES S17 links.Resolve + S07 verdict + recursive aggregate referenced-from-S18; NEVER forks/re-impls S18/S19/S22). hash via phase.Version()=records.KindPhase body S02. DONE-CRIT met: clean-text disjoint refund-eu/refund-us pair WITH red merged-cut mirror⇒CONFLICT refund∈conflicting BLOCKED requires_authority (fixture case1 + property5 text-never-overrides-red). Mappability §120: base.ID/ancestors must match else unresolvable+OpenQuestion (never guessed clean, no fabricated hash). conflict⇒requires_authority(override S16 human). go test ./archive/merge ok 5 fixture subcases(refund-conflict/cart-same-emergent-invariant-conflict/view-clean/no-ancestor-unresolvable/identity-clean-noop) + 8 rapid props(det+total/any-red⇒conflict/all-green⇒clean/identity-noop/text-never-overrides/conflict⇒named+authority/unresolvable-no-fab-clean/never-panics); go vet clean; full go build ./... clean prior-green intact. NO migration/hook/MCP/CLI verb (reuse prior steps, correct per spec); writes NO truth (read-only decider, clean ride S20 path). front lib/semantic-merge.ts faithful twin (mergedCutHash=LOCAL digest mirroring Go body SHAPE, Go hash authoritative=OQ when wired) + semantic-merge-data.ts EXAMPLES 3 triples (SAME as Go fixture, NOT in report... wait both ARE in files_changed) vitest 6/6 incl done-crit example test; SemanticMergePanel READ-ONLY action-capable pick+merge buttons run pure twin, e2e 4/4 testids match (merge-{refund,cart,view}, conflicting-${m}=conflicting-refund/conflicting-cart.own_mirror, FR conflit/propre/humain/git); tsc clean biome clean 5 files. nav WorkbenchHeader:114 semanticMerge. i18n 3441==3441 semanticMerge 28==28 parity-diff 0. skill /merge-semantic present. docs concept+internals 3 layers(Implémentation/Méta/Méta-méta) docs.json:119-120 mint-validate PASS pushed origin/main 9dc536c 0-ahead; code committed d01e396 clean tree. OQ: Go-hash-vs-local-digest-wiring / clean-merge→S20-ChangeSet-record-wiring(S26 curation) / Linear-unauth(needs OAuth+restart per project memory) all by-design. verified-green ZERO corrections.
