---
name: s19-weighted-propagation
description: S19 weighted/thresholded red propagation along composes + critical-needs-evidence; verified green
metadata:
  type: project
---

S19 = WEIGHTED, THRESHOLDED red propagation along the 7th composes link (KRD §112/§114; ADR 0018). PURE total deterministic engine back/kernel/propagation/propagation.go: closed 3-tier Weight {cosmetic|load-bearing|critical}, Activation 0/1/2 monotone, FireParent(Graph,parentID)→GREEN|RED (Σ activation of CHANGED children ≥ declared activation_threshold ⇒ RED; threshold≤0 means red-iff-activation>0; absent parent→GREEN invents no threshold), ValidateWeight(Link)→*BlockReason rejecting critical-without-evidence (CRITICAL_WEIGHT_WITHOUT_EVIDENCE how_to_fix [attach_incident_evidence, downgrade_to_load_bearing]) + UNKNOWN_WEIGHT outside closed set. §2463 backprop discipline: critical admitted ONLY on recorded weight_evidence (presence-only check; incident-existence lookup = later provenance OQ). Reads only declared weights/thresholds, writes no truth (the wall).

TWO DONE-CRITERIA MET (computed): (1) cosmetic change does NOT redden parent — FireParent(cart,{help-link})==GREEN fixture RowB + rapid TestCosmeticDoesNotRedden; (2) critical-no-evidence REJECTED — fixture RowG + rapid TestNoCriticalAdmissionWithoutEvidence. Per-link weights honored vs declared threshold; evidence required for critical; proven by BOTH mirror forms (fixture N2 8 rows A-H + rapid N1 4 props Total&Det/CosmeticDoesNotRedden/NoCriticalWithoutEvidence/MonotoneTierOrdering).

Migration kernel_propagation_baseline.sql EXPAND-ONLY: weight + weight_evidence ride INSIDE existing S17 kernel.link JSONB body (no ALTER, S17/S18 pattern) + NEW append-only side table kernel.composes_weight_admission with enum CHECK (cosmetic|load-bearing|critical) + conditional CHECK (weight<>'critical' OR weight_evidence IS NOT NULL) + READ-ONLY VIEW kernel.composes_weight; GRANT SELECT + REVOKE INSERT/UPDATE/DELETE/TRUNCATE aidos_agent = the wall. migration_roundtrip_test.go Testcontainers pg16 7 tests: CHECK-refuses-critical-no-evidence + inserts-with-evidence + cosmetic/load-bearing-no-evidence + UNKNOWN enum-refused + view roundtrip + expand-only prior tables untouched + AgentRoleSelectOnly INSERT permission-denied.

VERIFIED GREEN this cycle: gofmt clean, go vet clean, go test ./kernel/propagation/... 16.386s GREEN (fixture+property+Testcontainers), full go build ./... rc0 (prior green intact). Front lib/red-propagation.ts byte-faithful twin (same activation/fireParent/validateWeight) vitest 8/8, tsc rc0, biome clean 5 files. /red-propagation READ-ONLY Server Component ui-completeness VACUOUS (truth-write via ChangeSet) — weighted tree (thickness=§2362), live FIRE table, ADMISSION table; testids cart-parent/composes-edge[data-child,data-weight]/fire-row[data-verdict]/admission-row[data-row,data-result,data-code]/how-to-fix/tutorial/example (tutorial+example supplied by page.tsx, tables by component). e2e red-propagation.spec.ts 6 scenarios incl both done-criteria. nav WorkbenchHeader:105 redPropagation. i18n 3441==3441 redPropagation 34==34 nav.redPropagation both locales. Docs concept+internals 3 layers (Implémentation·Méta·Méta-méta) docs.json:107-108, clone clean 0-ahead origin/main (pushed). ADR 0018 present.

OQ by-design (non-blocking): critical-needs-evidence checks PRESENCE only (incident-existence lookup = later provenance step); no reweight SemanticDiff executor yet (later); no PostKernelChange red-wave wiring yet; weights/thresholds declared never learned; mirrors Postgres persist=S06 (fixture materialized valid); Linear MCP unauth (OAuth+restart). verified-green ZERO corrections.
