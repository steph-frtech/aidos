---
name: wb2-04-kernels
description: WB2-04 kernel composition tree screen — verified GREEN zero corrections; React Arborist SSRs a virtualized window
metadata:
  type: project
---

§WB2-04 (after WB2-03, spec WB2_PLAN.md) /v2/kernels = ARBRE DE COMPOSITION fractal des kernels (§17 `composes`, §49) React Arborist virtualisé read-only; déplier/replier drill-down + clic kernel→/v2/anatomie/[kernel] STUB(real anatomy=WB2-06 forward-dep). Twin lib/v2/kernel-tree.ts PURE buildKernelTree(composes plat→arbre ordonné)/validateComposes(empty_id/duplicate_id/level_unknown/facet_unknown/orphan_node/cycle)/flattenTree/countNodes/syntheticComposes(N); ordre DÉCLARÉ levelRank(SOURCE_ORDER §23 idx, bands→queue) puis id tie-break; REUSES isLevel+SOURCE_ORDER(besoin-grammar) isFacetLetter(lib/v2/idea→lib/facets 8 letters F/I/S/B/R/V/M/X); aucun orphelin GARANTI par validateComposes refusal AVANT build. KERNEL store n'expose pas encore composes réels→syntheticComposes(240) SYNTHETIC (OQ, seul KernelNode source changera). 

DONE-CRIT ALL MET: twin pur+ordonné+property (vitest 11/11: orphan refusé, compte préservé flatten===nodes.length, déterminisme même-entrée→même-sérialisation, profondeur racine=0 enfant=parent+1, fratries ordonnées verticale puis id, dup/cycle/coord-hors-jeu/empty refusés, 240-nœuds sans orphelin); e2e déplier/replier(data-open false→true)+200+ nœuds virtualisés(count 240, DOM<240)+clic→anatomie/k0; aucun nœud orphelin. KernelTreeClient useMemo syntheticComposes(240)→buildKernelTree→roots toArborist; Tree openByDefault=false height=520 overscan=8; KernelRow toggle btn(aria déplier/replier)+select btn→useState selected→Link /v2/anatomie/[id]. /v2/kernels(route étape) ET /v2/arbres(slug glossaire concept "Arbres" §124) deux segments statiques rendent KernelsScreen partagé, priment sur /v2/[slug], conservent contrat WB2-02 v2-concept-title="Arbres"+v2-concept-def.

VERIFIED-GREEN ZERO corrections: vitest kernel-tree 11/11, lib/v2 ALL 47/47(5 files glossary/schema/idea/lib-smoke/kernel-tree prior-green intact), biome 7 files clean, build "Compiled 12.8s" /v2/kernels+/v2/arbres+/v2/anatomie/[kernel] all ƒ; WALL clean(read-only projection, NO kernel/mirrors/fitness imports, NO fetch/POST/mutation, e2e writes===[]); determinism-first(buildKernelTree PURE authoritative, React Arborist=pure render no judge, ordre déclaré SOURCE_ORDER not learned, no LLM); i18n v2Kernels(7 keys)+v2Anatomie(5 keys) fr==en parity; docs 3-layer(Implémentation:9/Méta:30/Méta-méta:37) docs.json:497-498 mint validate PASS HEAD cafac87==origin/main clean.

SSR SURPRISE (contrast WB2-02 where Arborist was client-only): React Arborist DOES SSR a VIRTUALIZED WINDOW — curl :3404 /v2/kernels returns 25 node rows(k0..k238) all data-open="false", NOT all 240(proves virtualization in SSR too), all toggle/select/node testids present SSR'd; count NUMBER(240) hydrates client-side(static "nœuds dans l'arbre :" text SSRs, total renders client) so e2e count-240 assertion needs live hydrated server(executor ran prod build :3202 green); /v2/arbres SSRs concept-title="Arbres"+v2-kernels-tree, /v2/anatomie/k0 SSRs v2-anatomie-kernel-id="k0". tsc had 1 FALSE error in stale .next/dev/types/validator.ts(malformed gen artifact, .next gitignored)→rm -rf .next/dev then tsc EXIT0; SCAR-RESPECTED next start -p 3404 killed by exact lsof -ti pid, prod :3000 re-confirmed 200(NEVER pkill next).

OQ by-design(no residual): syntheticComposes SYNTHETIC til kernel store exposes composes / anatomie STUB→WB2-06 / Mintlify async propagation(URLs 404 at report, deploys mins) / Linear MCP unauth. Executor report fully accurate.
